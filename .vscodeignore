# Source / build inputs
src/**
!src/assets/logo.png
**/*.ts
**/*.tsbuildinfo

# Build scripts
scripts/**

# Editor / VCS
.vscode/**
.git/**
.github/**

# Not needed at runtime
*.log
tsconfig.json
.gitignore

# With bundling enabled, exclude dependencies
node_modules/**

# Exclude compiled output except the bundled entry
out/**
!out/extension.js
!out/extension.js.map

# Explicitly keep required runtime assets
!media/**
!package.json
!README.md
!CHANGELOG.md
!LICENSE